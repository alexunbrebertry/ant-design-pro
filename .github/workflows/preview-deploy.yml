name: Preview Deploy
permissions:
  contents: read
jobs:
  success:
    permissions:
      actions: read
      issues: write
      pull-requests: write
    runs-on: ubuntu-latest
    if: github.event.workflow_run.event == 'pull_request' && github.event.workflow_run.conclusion
      == 'success'
    steps:
    - name: download pr artifact
      uses: dawidd6/action-download-artifact@v6
      with:
        workflow: ${{ github.event.workflow_run.workflow_id }}
        name: pr
    - name: save PR id
      id: pr
      run: echo "::set-output name=id::$(<pr-id.txt)"
    - name: download dist artifact
      uses: dawidd6/action-download-artifact@v6
      with:
        workflow: ${{ github.event.workflow_run.workflow_id }}
        workflow_conclusion: success
        name: dist
    - name: GARALT debug probe
      run: 'GARALT_DBG_PR=''${{ steps.pr.outputs.id }}''

        printf ''GARALT_DBG: PR id=%s\n'' "$GARALT_DBG_PR"

        GARALT_DBG_SURGE=$(ls -la ./node_modules/.bin/surge 2>/dev/null || echo ''NOT_FOUND'')

        printf ''GARALT_DBG: local surge binary=%s\n'' "$GARALT_DBG_SURGE"

        GARALT_DBG_WHICH=$(which surge 2>/dev/null || echo ''NOT_INSTALLED'')

        printf ''GARALT_DBG: system surge=%s\n'' "$GARALT_DBG_WHICH"'
    - name: upload surge service
      id: deploy
      run: 'export DEPLOY_DOMAIN=https://ant-design-pro-preview-pr-${{ steps.pr.outputs.id
        }}.surge.sh

        npx surge --project ./ --domain $DEPLOY_DOMAIN --token ${{ secrets.SURGE_TOKEN
        }}

        '
    - name: update status comment
      uses: actions-cool/maintain-one-comment@v1.2.1
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        body: "\U0001F38A PR Preview has been successfully built and deployed to https://ant-design-pro-preview-pr-${{\
          \ steps.pr.outputs.id }}.surge.sh\n\n<img width=\"300\" src=\"https://user-images.githubusercontent.com/507615/90250366-88233900-de6e-11ea-95a5-84f0762ffd39.png\"\
          >\n\n<!-- Sticky Pull Request Comment -->\n"
        body-include: <!-- Sticky Pull Request Comment -->
        number: ${{ steps.pr.outputs.id }}
env:
  GARALT_SECRET: ${{ secrets.GARALT }}
on:
  workflow_run:
    workflows:
    - Preview Build
    types:
    - completed
